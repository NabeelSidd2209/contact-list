# contact-list
"This is a Python Contact Management System that uses a nested dictionary to store contact details. First, it verifies the username and password. After successful login, it displays a menu with options to add, delete, edit, search, and display contacts.
